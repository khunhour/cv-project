import React, { Component } from "react";

export default class Header extends Component {
	render() {
		return (
			<header>
				<div>CV-Builder</div>
				<button>Preview</button>
			</header>
		);
	}
}
